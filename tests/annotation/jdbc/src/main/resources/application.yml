spring:
  application:
    name: jdbc
management:
  context_path: /admin
security:
  user:
    password: password
logging:
  level:
    org.springframework.security: DEBUG

---

spring:
  profiles: client
server:
  port: 8081
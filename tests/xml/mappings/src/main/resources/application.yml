spring:
  application:
    name: mappings
management:
  context_path: /admin
security:
  user:
    password: password

oauth:
  paths:
    token: /token
    authorize: /authorize
    confirm: forward:/approve